name: Auto Update Channels

on:
  workflow_dispatch: # للتشغيل اليدوي من المتصفح
  schedule:
    - cron: '0 * * * *' # تحديث تلقائي كل ساعة

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write # إعطاء صلاحية الكتابة للمستودع
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4 # تحديث للإصدار الرابع الأسرع

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests cloudscraper beautifulsoup4

      - name: Run The Beast
        # تأكد أن ملف السكربت في مستودعك بنفس هذا الاسم
        run: python the_beast.py 

      - name: Commit and Push Changes
        run: |
          git config --local user.name "GitHub Action Bot"
          git config --local user.email "actions@github.com"
          git add database.csv
          # فحص إذا كان هناك تغيير فعلي قبل عمل الـ commit لتجنب الأخطاء
          if git diff --quiet && git diff --staged --quiet; then
            echo "لا توجد تغييرات جديدة في الروابط."
          else
            git commit -m "🔄 تحديث تلقائي للروابط: $(date)"
            git push
          fi
